<script lang="ts">
  export let minutes: number;
  export let increment: number;

  // const averageMoveCount = 40;
  // $: averageTotalTime = 2 * (minutes*60 + increment* averageMoveCount) / 60;
  // $: averageTimeFormated = Math.round(averageTotalTime * 10) / 10;
</script>
